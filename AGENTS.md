# Development Instructions

Use `uv` for all Python-related work. Create and manage the environment, install dependencies, run tools, tests, and the application through `uv`; do not invoke `pip` or a bare Python interpreter directly.
